#=========================================================
#
#	msgedit用のサウンドネームメッセージ生成
#
#	sound_data.sadlから"SEQ"のついている定義を抜きだす
#	→tmp.txtに出力
#
#=========================================================
{
	if( $2 ~ "SEQ" ){
		#name = $2
		#sub( "_", "-", name )
		#print name

		#文字コード"_"が存在しない(050914)
		#このまま出力して、最後に変換をかける
		print $2
	}
}

